"use client";
import { useFormStatus } from "react-dom";
import { markQuestComplete, saveReflection } from "@/app/actions/bootcamp";
function SubmitButton({ label }: { label: string }) { const { pending } = useFormStatus(); return <button className="rounded-full bg-cyan-400 px-5 py-3 font-semibold text-slate-950 disabled:opacity-60" disabled={pending}>{pending ? "Saving..." : label}</button>; }
export function QuestActions({ questId, reflection }: { questId: string; reflection?: string | null }) { return <div className="space-y-4"> <form action={saveReflection} className="space-y-3 rounded-3xl border border-white/10 bg-white/5 p-5"> <input type="hidden" name="questId" value={questId} /> <textarea name="reflection" defaultValue={reflection ?? ""} placeholder="Write a short reflection..." className="min-h-32 w-full rounded-2xl border border-white/10 bg-slate-950/40 p-4" /> <SubmitButton label="Save reflection" /> </form> <form action={markQuestComplete} className="rounded-3xl border border-cyan-400/20 bg-cyan-400/10 p-5"> <input type="hidden" name="questId" value={questId} /> <input type="hidden" name="reflection" value={reflection ?? ""} /> <SubmitButton label="Mark complete" /> </form> </div>; }
