# Day 7–8 Exercise: Loops and Lists
#
# Fill in each function so the tests pass.
# Run: python3 -m pytest exercises/phase1_core/test_day07_loops.py


def sum_to_n(n: int) -> int:
    """Return the sum of all integers from 1 up to and including n.

    Example:
        sum_to_n(5)  -> 15   (1+2+3+4+5)
        sum_to_n(3)  -> 6    (1+2+3)
    """
    # TODO: replace `pass` with your code
    pass


def even_numbers(max_n: int) -> list:
    """Return a list of all even numbers from 2 up to and including max_n.
    If max_n is odd, stop at max_n - 1.

    Example:
        even_numbers(8)   -> [2, 4, 6, 8]
        even_numbers(7)   -> [2, 4, 6]
        even_numbers(2)   -> [2]
    """
    # TODO: replace `pass` with your code
    pass


def reverse_list(items: list) -> list:
    """Return a new list that is the reverse of items.

    Example:
        reverse_list([1, 2, 3])  -> [3, 2, 1]
        reverse_list(["a", "b"]) -> ["b", "a"]
    """
    # TODO: replace `pass` with your code
    pass


def count_occurrences(items: list, target) -> int:
    """Count how many times target appears in items.

    Example:
        count_occurrences([1, 2, 2, 3], 2)   -> 2
        count_occurrences(["a", "b", "a"], "a") -> 2
        count_occurrences([1, 2, 3], 9)        -> 0
    """
    # TODO: replace `pass` with your code
    pass
