# Day 5–6 Exercise: Conditions and Boolean Logic
#
# Fill in each function so the tests pass.
# Run: python3 -m pytest exercises/phase1_core/test_day05_conditions.py


def age_category(age: int) -> str:
    """Classify an age into one of three categories.

    Return:
        "kid"       if age < 13
        "teenager"  if 13 <= age < 20
        "adult"     if age >= 20

    Example:
        age_category(8)   -> "kid"
        age_category(15)  -> "teenager"
        age_category(25)  -> "adult"
    """
    # TODO: replace `pass` with your code
    pass


def is_even(number: int) -> bool:
    """Return True if number is even, False if it is odd.

    Example:
        is_even(4)  -> True
        is_even(7)  -> False
    """
    # TODO: replace `pass` with your code
    pass


def fizzbuzz(n: int) -> str:
    """Classic FizzBuzz for a single number.

    Return:
        "FizzBuzz" if n is divisible by both 3 and 5
        "Fizz"     if n is divisible by 3 only
        "Buzz"     if n is divisible by 5 only
        str(n)     otherwise

    Example:
        fizzbuzz(15) -> "FizzBuzz"
        fizzbuzz(9)  -> "Fizz"
        fizzbuzz(10) -> "Buzz"
        fizzbuzz(7)  -> "7"
    """
    # TODO: replace `pass` with your code
    pass
